# Swift / SPM
client/.build/
client/.swiftpm/
client/Package.resolved

# Python
server/__pycache__/
server/*.pyc
server/venv/
server/.env

# macOS
.DS_Store
*.swp
*~

# IDE
.vscode/
.idea/
*.xcodeproj/
*.xcworkspace/

# Claude Code
.claude/

# Internal docs (not for public)
PLAN.md
WE入口进展.md
build-in-public.md
server/eval/EVAL_FRAMEWORK.md
server/eval/FINETUNE_PIPELINE.md
server/eval/We数据评估*.md

# Datasets (too large, download separately)
Datasets/
server/eval/datasets/

# Data (user-specific)
*.jsonl
*.offset

# User config (contains API keys)
config.json

# Secrets
*.pem
*.p12
*.key
__pycache__/

# Local benchmark fixtures + media (kept out of git — large/private meeting audio)
client/.fixtures/
videoplayback.mp4

# Sparkle generate_appcast local backup of superseded update files
docs/downloads/old_updates/

# Playwright temp + local test artifacts
.playwright-mcp/
meeting-temp.wav
client/scripts/kpi-test/data/short/audio/
client/scripts/kpi-test/data/medium/audio/
client/scripts/kpi-test/data/long/audio/
client/scripts/kpi-test/data/meeting/*.wav
client/scripts/kpi-test/results/latest/

# KPI test manifests + results — override the global *.jsonl rule
!client/scripts/kpi-test/data/*/manifest.jsonl
!client/scripts/kpi-test/results/**/*.jsonl
